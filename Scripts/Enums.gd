extends Node

enum ButtonColor {
	RED,
	GREEN,
	YELLOW,
}

enum DoorColor {
	RED,
	GREEN,
	YELLOW,
}

enum TrapdoorColor {
	RED,
	GREEN,
	YELLOW,
}

enum ShellType {
	NORMAL,
	NONE,
	PUFFERFISH,
	SWORDFISH,
}
